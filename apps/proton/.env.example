# ── Google Cloud / Vertex AI ───────────────────────────
# Your GCP project ID (required for Gemini via Vertex AI)
GCP_PROJECT_ID=your-gcp-project-id

# GCP region (defaults to us-central1)
GCP_LOCATION=us-central1

# (Optional) Path to a service account JSON key file.
# If omitted, the Vertex AI SDK uses Application Default Credentials (ADC):
#   1. Local dev  → run `gcloud auth application-default login` once
#   2. On GCP     → uses the attached service account automatically
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ── Anthropic / Claude ─────────────────────────────────
ANTHROPIC_API_KEY=your-anthropic-api-key

# ── Google AI / Generative Language API ────────────────
# Required for models not on Vertex AI (e.g. gemini-3-flash-preview)
# Get your key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
